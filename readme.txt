Steps to follow:

1. Move to the code folder.
2. Open terminal window and run 
	gcc Client.c -o Client; 
	gcc Server.c -o Server;
	./Client
3. Open another terminal window and run
	./Server
4. Give inputs as per instructed
